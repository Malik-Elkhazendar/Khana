# booking-engine

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build booking-engine` to build the library.

## Running unit tests

Run `nx test booking-engine` to execute the unit tests via [Jest](https://jestjs.io).
